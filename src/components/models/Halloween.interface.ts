export interface CardItemEvents {
    icon: string;
    description: string;
    title: string;
    footer: string;
}

export interface CardItemCostumes {
    icon: string;
    title: string;
    footer: string;
}